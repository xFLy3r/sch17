const application = require('./application');
const admin = require('./admin');
const routes = require('./routes');
const db = require('./db');

module.exports = {
    application,
    admin,
    routes,
    db
}